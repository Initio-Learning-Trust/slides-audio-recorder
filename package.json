{
  "name": "slides-audio-recorder",
  "version": "1.0.0",
  "private": true,
  "description": "Google Workspace Marketplace add-on that records audio in the Google Slides sidebar and places it on a slide.",
  "license": "Apache-2.0",
  "scripts": {
    "build": "node tools/sync-protocol.js && node tools/sync-brand.js",
    "test": "node --test \"test/*.test.js\"",
    "verify": "npm run build && git diff --exit-code -- apps-script/Protocol.html apps-script/Brand.html && npm test && npm run test:e2e",
    "push": "clasp push",
    "open": "clasp open",
    "test:e2e": "node --test \"test/e2e/*.e2e.js\""
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2",
    "playwright": "^1.63.0"
  },
  "engines": {
    "node": ">=18"
  }
}
